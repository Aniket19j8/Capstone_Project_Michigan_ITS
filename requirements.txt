# Root stack deps — Ollama does local embeddings; Chroma stores vectors
chromadb>=1.5.1
pysqlite3-binary>=0.5.0   # old school clusters ship ancient sqlite; 03 patches this
sentence-transformers>=5.1.0   # CrossEncoder in 04 — pin 5.1.x if you're still on py3.9
transformers>=4.41.0
torch>=2.2.0
rank_bm25>=0.2.2
huggingface_hub>=0.23.0

# HTTP / API glue
requests>=2.32.0
fastapi>=0.111.0
uvicorn>=0.30.0
pydantic>=2.7.0
python-dotenv>=1.0.0

# wrangling + plots for eval scripts
pandas>=2.2.0
numpy>=1.26.0
scikit-learn>=1.5.0
matplotlib>=3.9.0
tqdm>=4.66.0

# optional: pip install datasets  ← only if you run 01_download_data.py --with-public-datasets

# DB drivers we touched for RDS experiments
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9

# S3 upload helper (batch jobs)
boto3>=1.34.0

# CLI niceties
typer>=0.12.0
rich>=13.7.0
jsonlines>=4.0.0
