# Optional Phase 6 LoRA training dependencies.
# Install only on the AWS training instance, not for normal app/runtime usage.

accelerate>=0.31.0
bitsandbytes>=0.43.1
datasets>=2.20.0
peft>=0.11.1
trl>=0.9.6
transformers>=4.41.0
torch>=2.2.0
sentencepiece>=0.2.0
protobuf>=4.25.0
